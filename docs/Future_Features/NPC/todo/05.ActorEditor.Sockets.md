# 05. Сокеты

> [!IMPORTANT]
> Работа ведется исключительно в ветке **feat/actor-editor**.

## 1. Установка и Манипуляция

- [x] Режим установки сокета (Ray-cast клик по мешу).
- [x] Визуализация: Зеленое кольцо (Torus) + Желтый указатель направления (Pin).
- [x] Stabilization of 3D Translation Gizmos (Manual raycast, axis locking, world-alignment)
- [ ] **Вращение Сокетов (Rotation System)**:
  - [ ] Реализовать гизмо вращения (3 кольца: Red-X, Green-Y, Blue-Z).
  - [ ] Добавить визуализацию направления `Forward` (локальная ось Z) через стрелку или конус.
  - [ ] Разработать систему ручного вращения через рейкаст (вычисление угла по движению мыши в плоскости гизмо).
  - [ ] Синхронизировать `transform.rotation` с метаданными сокета.
  - [ ] Добавить возможность сброса вращения (Reset Rotation) в UI.
- [ ] ~~Интеграция 3D-Gizmo (`bevy_transform_gizmo`) для перемещения и вращения.~~ Используется [/crates/client_core/src/actor_editor/systems/gizmos.rs](../../../../crates/client_core/src/actor_editor/systems/gizmos.rs)
- [x] Синхронизация: Изменения в 3D применяются к `ActorSocket`.

## 2. Метаданные и Валидация

- [ ] Реализовать UI-форму для редактирования Имени и Комментария.
- [ ] **Валидация имен**:
  - [ ] При попытке дать имя, которое уже есть в текущей группе (например, две "Eye_L" в голове) — выдавать ошибку или добавлять индекс.
- [x] Автоматическая привязка сокета к ближайшей части (Head/Body/Engine) при создании. (Реализовано через ECS Hierarchy — сокет является Child-сущностью части меша).

## 3. Socket List UI

- [x] Динамический список всех сокетов в боковой панели.
- [x] Подсветка выбранного сокета (в списке и в 3D).
- [x] Выбор сокета кликом по 3D-модели (через `bevy_mod_picking`).
- [x] Отображение текущих координат (X, Y, Z) выбранного сокета.
- [ ] Поиск/Фильтрация сокетов по имени (требуется виджет текстового ввода).

## 4. Визуализация

- [ ] Отрисовка сокетов в виде 3D-иконок или стрелок, показывающих направление (Forward).
- [ ] Цветовая кодировка сокетов по группам.

## 4. Система спецэффектов (VFX)

- [ ] Определить структуру `EffectConfig` в `shared` для описания параметров (тип, цвет, скорость).
- [ ] Реализовать загрузку пресетов эффектов из внешних файлов (например, `.ron`).
- [ ] Создать базовый визуализатор (эмиттер частиц или шейдерный эффект) для превью в сокете.
- [ ] Добавить в UI редактора возможность выбора и настройки эффекта для каждого сокета.
- [ ] Реализовать опцию «Show/Hide Previews» в глобальных настройках визуализации.
