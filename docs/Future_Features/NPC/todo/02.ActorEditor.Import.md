# ~~02. Импорт модели~~

> [!IMPORTANT]
> Работа ведется исключительно в ветке **feat/actor-editor**.

## 1. Выбор файла

- [x] Интегрировать диалоговое окно выбора файла (например, через `rfd` или встроенный в `egui` файловый менеджер).
- [x] Ограничить выбор форматами: `.gltf`, `.glb`, `.obj`.

## 2. Загрузка в Bevy

- [x] Использовать `AssetServer` для асинхронной загрузки модели.
- [x] Реализовать "Loading Screen" или индикатор загрузки.
- [x] После загрузки:
  - [x] Вычислить Bounding Box модели.
  - [x] Автоматически отмасштабировать и центрировать модель во вьюпорте.
  - [x] Извлечь исходную геометрию (Mesh) для хранения в `OriginalMeshComponent`.
