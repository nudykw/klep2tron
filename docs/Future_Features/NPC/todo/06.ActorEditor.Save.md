# 06. Сохранение проекта

> [!IMPORTANT]
> Работа ведется исключительно в ветке **feat/actor-editor**.

## 1. Экспорт в GLB
- [ ] Реализовать конвертацию Bevy-мешей в структуру GLTF через `gltf-json`.
- [ ] Запись данных о сокетах в иерархию Nodes.
- [ ] Запись комментариев в `extras`.
- [ ] Сохранение трех файлов: `Head.glb`, `Body.glb`, `Engine.glb`.

## 2. Файл проекта
- [x] Сериализация структуры `actor.ron` через `serde` и `ron`.
- [x] Состав RON:
    - `name: String`
    - `source_path: String`
    - `cut_top: f32`, `cut_bottom: f32`
    - `config: ActorConfig { sockets: Vec<SocketDefinition> }`
- [ ] Добавление `material_settings: Map<String, MaterialInfo>` (в процессе).

## 3. Дополнительные задачи
- [x] **Undo/Redo**: Финализация системы — сохранение истории действий в памяти для работы кнопок отката.
- [x] **Автоматическое создание структуры папок**: `assets/actors/{ActorName}/`.
- [x] **UX: Модальное окно именования**: Запрос имени при первом сохранении с автогенерацией на базе файла.
- [x] **UX: Разрешение конфликтов**: Проверка существования проекта на диске перед первым сохранением с отображением метаданных (дата, модель, сокеты).
