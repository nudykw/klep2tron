> [!IMPORTANT]
> Работа ведется исключительно в ветке **feat/actor-editor**.

# 06. Сохранение проекта

## 1. Экспорт в GLB
- [ ] Реализовать конвертацию Bevy-мешей в структуру GLTF через `gltf-json`.
- [ ] Запись данных о сокетах в иерархию Nodes.
- [ ] Запись комментариев в `extras`.
- [ ] Сохранение трех файлов: `Head.glb`, `Body.glb`, `Engine.glb`.

## 2. Файл проекта
- [ ] Сериализация структуры `project.json` через `serde`.
- [ ] Состав JSON:
    - `source_model_path: String`
    - `cut_top: f32`, `cut_bottom: f32`
    - `sockets: Vec<SocketData>`
    - `material_settings: Map<String, MaterialInfo>`

## 3. Дополнительные задачи
- [ ] **Undo/Redo**: Финализация системы — сохранение истории действий в памяти для работы кнопок отката.
- [ ] Реализация автоматического создания структуры папок: `assets/NPC/{ActorName}/`.
