# 10. Режим инспекции (Post-Slicing)

> [!IMPORTANT]
> Данный этап выполняется ПОСЛЕ реализации разрезания модели (задача 04).

## 1. Визуализация плоскостей

- [x] Реализовать отрисовку плоскостей разреза как полупрозрачных мешей.
- [x] Динамическое обновление положения плоскостей при изменении параметров `cut_top` / `cut_bottom` (Слайдеры с подтверждением реализованы).

## 2. Инспекция частей (Head/Body/Engine)

- [x] При активации режима инспекции конкретной части:
  - [x] **Solo Mode**: Скрывать остальные части модели или переводить их в режим **Ghosting** (прозрачность 0.1).
  - [x] **Focus**: Центрировать камеру на Bounding Box выбранной части с плавным переходом.
  - [x] Ограничивать вращение камеры вокруг центра этой части (через смену фокуса PanOrbitCamera).
- [x] **UI Управление (Секция PARTS)**:
  - [x] Добавить кнопки "Focus" и "Solo" (Solo активирует Ghosting).
  - [x] Добавить тулбар для переключения **Wireframe** и **Normals Debug** (1% плотности для чистоты).
  - [x] **Master Toggle**: Главный выключатель в заголовке секции для мгновенного сброса всех эффектов.
- [x] **Интеллектуальный UI**:
  - [x] **Синхронизация**: При включении режима инспекции секция PARTS разворачивается автоматически.
  - [x] **Начальное состояние**: При старте развернута только MATERIALS, остальные секции свернуты.
  - [x] **Горячие клавиши**: Клавиша `I` переключает части циклично. Добавлена проверка на `Ctrl` для исключения конфликта с импортом.
- [x] **Верификация геометрии**:
  - [x] Проверить визуальную целостность "заглушек" (caps).
  - [x] Использовать отображение нормалей (1% плотности) для проверки их корректной ориентации на срезах.
- [x] **Интерактивность**: Реализовать подсветку части в 3D (emissive) при наведении на её имя в списке инспектора.

## 3. Статус реализации: ГОТОВО

Все функциональные требования по инспекции NPC Actor выполнены. Система интегрирована в общий пайплайн редактора.
