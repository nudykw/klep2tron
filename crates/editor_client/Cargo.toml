[package]
name = "editor_client"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.19"
bevy_obj = "0.19"
serde_json = "1"
client_core = { path = "../client_core" }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
actor_editor = { path = "../actor_editor" }

[build-dependencies]
chrono = "0.4"
