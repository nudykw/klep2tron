[package]
name = "editor_client"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
bevy = "0.14"
bevy_obj = "0.14"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
chrono = "0.4"
